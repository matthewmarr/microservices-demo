# Default values for helm_chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: gcr.io/andresolarte-istio-pg/time-lady
  tag: 93a7deb
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080
